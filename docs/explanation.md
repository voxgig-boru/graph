# Explanation

> **Status: the library is designed, not implemented.** `graph.aql`
> exports an empty `Graph` namespace, so there is no API to document
> yet. See **[DESIGN.md](../DESIGN.md)** for the argued plan.

The design rationale currently lives in full in [DESIGN.md](../DESIGN.md) — why this is a separate library from `sort`, why one engine serves Dijkstra and A\*, and which boru constraints forced each shape. This page will carry the user-facing half of that once there is an API to explain.
